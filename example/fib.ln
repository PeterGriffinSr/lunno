import "core:io"

let fib(n) {
    let fib_aux(m, a, b) {
        match m {
            | 0 -> a
            | _ -> fib_aux(m - 1, b, a + b)
        }
    }
    fib_aux(n, 0, 1)
}

let main {
    io.println(fib(10))
}